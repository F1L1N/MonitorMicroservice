@model IEnumerable<MonitorMicroservice.Models.Patient>
@{
    ViewBag.Title = "Список пациентов";
}
<a asp-action="Create">Зарегистрировать пациента</a>
<table class="table">

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>
                <a asp-action="Details" asp-controller="Patient" asp-route-id="@item.Id">Подробнее</a> |
                <a asp-action="Edit" asp-controller="Patient" asp-route-id="@item.Id">Изменить</a> |
                <a asp-action="Delete" asp-controller="Patient" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
    }
</table>