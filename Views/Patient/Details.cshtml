@model MonitorMicroservice.Models.Patient
@{
    ViewBag.Title = Model.Name;
}
<h2>Пациент @Model.Name</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Идентификатор</dt>
        <dd>@Model.Id</dd>

        <dt>Возраст</dt>
        <dd>@Model.Age</dd>
    </dl>
</div>